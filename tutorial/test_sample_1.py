import pytest


def test_file1_method_1():
    x, y = 5, 6
    assert x + 1 == y, "Test failed..."
    assert x == y, "Test failed..."


def test_file_1_method_2():
    x, y = 5, 6
    assert x + 1 == y, "Test failed..."
